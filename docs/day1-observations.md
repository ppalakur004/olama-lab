The shortest case E12 used 228 input tokens and 4716 ms; the longest case E11 used 274 input tokens and 4533 ms. Input tokens rose with document length, but latency did not: E12 was slightly slower despite the smaller prompt. Estimating model workload from only the short document would understate token use on longer cases and would treat latency as if it scaled with input size, which this run does not show.
